import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFF6C63FF);       // Purple accent
  static const secondary = Color(0xFF4A44B7);     // Darker purple
  static const accent = Color(0xFFF8B500);        // Bright yellow
  static const background = Color(0xFFF5F5F5);    // Light gray background
  static const textDark = Color(0xFF333333);      // Dark text for readability
  static const textLight = Color(0xFFF5F5F5);     // Light text for dark backgrounds
  
  static const textOnDark = Color(0xFFF5F5F5);    // Light text on dark backgrounds
  static const cardDark = Color(0xAA2D2D2D);      // Dark gray with opacity for card backgrounds
  static const iconOnDark = Color(0xFF6C63FF);    // Purple accent for icons on dark backgrounds
}
